@model FootbalManager_UIL.ContinentPaysViewModel

@{
    ViewBag.Title = "GetPays";
}

<h2>GetPays</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("IdPays")
        </th>
        <th>
            @Html.DisplayName("NomPays")
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.ListePaysVM)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdM)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomPaysM)
            </td>
            <td>
                @Html.ActionLink("Choisir", "GetEquipe", new { idContinent = item.FkContinentM, idPays = item.IdM }) 
                
            </td>
        </tr>
    }

</table>
<div>
    @Html.ActionLink("Back", "Index", new { /* id=item.PrimaryKey */ })
</div>
